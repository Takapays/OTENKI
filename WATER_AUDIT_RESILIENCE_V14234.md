# V1.4.234 水場監査耐障害化

- 300山を25座単位で処理し、各チャンク完了後にキャッシュをcommit/push。
- `--start` / `--limit` / `--resume` / `--errors-only` を追加。
- 1リクエストの待ち時間を既定10秒、試行回数2回に上限化。
- Overpass APIは複数エンドポイントへ切替可能。
- 初回全体後に未解決山だけを1回再試行。
- `nwr` を使い node/way/relation を4条件で取得し、従来7条件/地点よりクエリを軽量化。
- dry-runで固定監査座標300/300を確認。
