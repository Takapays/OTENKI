# V1.5.19 UI主導線・宿泊翌朝時刻調整

## UI
- 「代表コースを読み込む」を濃いグリーンの主操作色へ変更。
- 「コースを自分で設計」は従来の代表コース側の淡いグリーンへ変更。
- 前回ルート欄の「前回の山行」見出しを削除し、メタ情報のみ残す。
- 「前回ルートを復元」を「前回作成ルート復元」へ変更。

## 宿泊翌朝
- 通常の山行設定で宿泊をONにした際の翌朝出発デフォルトを 06:00 に変更。
- 保存ルート復元時に翌朝時刻が未保存の場合も 06:00 を使用。
- 王道ルートの宿泊候補も翌朝 06:00 に統一。
- 既に保存済みで 05:00 等の時刻を明示保存しているルートは、その保存値を優先して復元する。

## 非変更
- CTデータ、CT resolver、王道ルート定義、気象分析ロジックは変更なし。
- 朝5時の空など気象評価用の 05:00 基準は宿泊出発時刻とは別物のため変更していない。
