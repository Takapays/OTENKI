# Traten V1.6.61

Base: V1.6.60

## Changes
- Restore permanent access to the Saved Routes list even when no previous-route snapshot exists.
- Hide only the previous-route restore button when no previous-route snapshot exists; Saved Routes remains available.
- Add the selected nationwide forecast date to the hourly forecast heading (example: `時間別予測 ｜ 2026/09/21`).
- No forecast grade logic, WeatherAPI logic, Supabase fallback logic, Instagram logic, or mountain data changes.
