(()=>{'use strict';const db=window.TRATEN_TRAILHEAD_ACCESS_DB||{};const rows=Object.entries(db).map(([name,v])=>({name,...v}));const q=document.getElementById('q'),area=document.getElementById('area'),list=document.getElementById('list'),summary=document.getElementById('summary');const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));[...new Set(rows.map(x=>x.area).filter(Boolean))].sort((a,b)=>a.localeCompare(b,'ja')).forEach(v=>area.insertAdjacentHTML('beforeend',`<option value="${esc(v)}">${esc(v)}</option>`));function render(){const term=q.value.trim().toLowerCase(),a=area.value;const hit=rows.filter(x=>(!a||x.area===a)&&(!term||[x.name,x.area,x.prefecture,...(x.aliases||[])].join(' ').toLowerCase().includes(term)));summary.innerHTML=`<span>登録 ${rows.length}地点</span><span>表示 ${hit.length}地点</span>`;list.innerHTML=hit.length?hit.map(x=>`<article class="card"><h3>${esc(x.name)}</h3><div class="meta">${esc(x.area||'')} · ${esc(x.prefecture||'')}</div><p><span class="tag">車</span>${esc(x.nearestIC||'—')} / ${esc(x.drive||'—')}</p><p><span class="tag">駐車</span>${esc(x.parking||'情報なし')}</p><p><span class="tag">公共交通</span>${esc(x.transit||'情報なし')}</p><p>${esc(x.roadNote||'')}</p>${(x.links||[]).map(l=>`<a href="${esc(l[1])}" target="_blank" rel="noopener noreferrer">${esc(l[0])} ↗</a>`).join('　')}</article>`).join(''):`<div class="empty">該当する登山口がありません。</div>`}q.addEventListener('input',render);area.addEventListener('change',render);render()})();
