# V1.5.5 水場拡張監査 V2 — アルプス＋八ヶ岳

## 目的
既存の固定水場DBを壊さず、水場候補の取りこぼしを増分で洗い出すための「候補専用」監査系を追加した。
候補は自動的に本番水場へ登録しない。公開座標と出典を人が確認した後だけ固定DBへ昇格する。

## 対象
日本三百名山のうち65座。
- 北アルプス: 34座
- 中央アルプス: 6座（経ヶ岳（長野）/ 木曽駒ヶ岳 / 空木岳 / 南駒ヶ岳 / 越百山 / 安平路山）
- 南アルプス: 22座
- 八ヶ岳: 3座（蓼科山 / 天狗岳 / 八ヶ岳（赤岳））

## OSM候補抽出
従来の代表地点周辺検索より高感度化。
- 代表ルート配列を保持できる31座: ルートを約650m間隔で補間し、750m半径の帯として検索。
- ルート配列を安全に復元できない34座: 既存の固定通過地点だけをバッファ検索し、無関係な地点同士を直線で結ばない。
- 候補タグ: `amenity=drinking_water`, `natural=spring`, `man_made=water_tap`, `man_made=water_well`, `amenity=water_point`, `fountain=drinking`, `drinking_water=*`。
- ルートから1600m超の候補は除外。
- ルート距離、飲用タグ、種別、名称でスコアリング。
- 「温泉」「足湯」等は、明示的な飲用タグがなければ疑義候補へ降格。

## 各種公開情報の取り込み
`water-v2-public-seeds.csv` を追加。
YAMAP、ヤマレコ、自治体、山小屋、観光協会等で「名称・公開座標・出典URL」を確認できた地点だけ候補キューへ追加できる。
座標推測は禁止。出典URL必須。
初期seedとして白馬岳「銀嶺水」を登録済み。

## 保存先
生成する候補キューは通常main・リリースZIPへ入れず、専用 `water-candidates` ブランチへ保存する。
- `water-v2-candidates.json`
- `WATER_V2_CANDIDATES.csv`
- `WATER_V2_CANDIDATES.md`

GitHub Actions `Water V2 candidate audit (Alps + Yatsugatake)` は手動実行専用。
既に監査済みの山はresumeで飛ばし、既存固定水場との90m以内重複も表示上区別する。

## 昇格ルール
V2候補は候補のまま。本番 `water-mountain-cache.json` へ自動反映しない。
最低限、名称・座標・出典の妥当性を確認してから固定水場として採用する。
