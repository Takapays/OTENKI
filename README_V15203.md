# Traten V1.5.203

## Fix
- Recalibrated the scene-1 nationwide map marker projection.
- The previous version plotted A/B/C markers into a box that was too small and too low inside the approved design template, so markers appeared shifted.
- Adjusted the Japan map projection bounds to the full footprint of the actual template map and slightly reduced marker size to improve overlap in dense areas.

## Effect
- `静止画をプレビュー` page 1 marker positions are better aligned with the map.
- `リールをプレビュー` uses the same corrected page 1.
