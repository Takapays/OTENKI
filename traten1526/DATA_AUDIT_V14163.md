# V1.4.163 データ監査画面

管理用 `/data-audit` を追加。

監査対象:
- 座標未確認: 固定候補または代表コース上で座標解決できない地点
- 推定CT: 固定座標・標高差＋確認済みCT回帰による推定値を使う代表コース区間
- CT情報なし: 代表コース上でCTを取得できない区間
- 代表コースなし: 手動・自動生成のどちらでも代表コースがない山
- 通過ポイント少なめ: 座標確定済み固定ポイントが3地点以下の山

監査値は `app.js` の現行データから画面表示時に再計算するため、固定データ更新に追随する。
