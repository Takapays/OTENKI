# Traten V1.5.216

## Instagram Bot first-slide update
- Replaced `M/D 明日の登山コンディション` with a large date-only label: `M/D（曜）`.
- Weekday is calculated automatically from the forecast target date in Japanese (`月`〜`日`).
- Enlarged the date and moved it slightly upward so it is the dominant subheadline under `日本三百名山 全国分析`.
- Applied the same date treatment to both the dynamic static-image page 1 and the Reel page 1 fallback renderer.
- Preserves the approved V1.5.214 A/B/C placement, JST `時点` stamp, and `作成日時` stamp.
- Bumped Reel render revision to invalidate previously cached Reel/static page-1 assets.
- Includes all V1.5.215 Reel preview crash fixes.
