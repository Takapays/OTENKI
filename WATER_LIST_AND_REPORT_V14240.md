# V1.4.240 水場一覧 + ユーザー操作時レポ検索

1. 先に固定水場一覧を300山へ拡張する。固定監査はOSM/Overpassのみで、未監査・通信失敗は「水場なし」にしない。
2. 一覧の各水場に「レポを探す」を表示する。
3. レポ検索はページロードでは実行しない。ユーザーが押した水場1件だけ `/api/water-source-report` で検索する。
4. 検索結果は公開検索結果のタイトル・抜粋・見える日付・元ページリンクのみ。記事本文は取得・転載しない。
5. OSMの `drinking_water` 属性は固定登録情報であり、現在の出水・飲用安全を保証しない。最新レポートと明確に分ける。
6. GitHub Actionsは1山1クエリ、10山/回、3時間ごとのローテーション。確認済み行を保持し、未解決のみ再試行する。
