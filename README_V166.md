# Traten V1.6.6

V1.6.5からのPC版代表コースUI修正。

- PC版では代表コース候補を常時表示せず、「代表コースを読み込む」にマウスオーバー／キーボードフォーカスした時だけ右側へ表示。
- 代表コース候補の文字サイズを縮小。
- 「代表コースを読み込む」のtitleツールチップを廃止。
- 代表コース読込後は「コースを自分で設計」を白背景で無効化。
- 山または代表コースを選び直すと手動設計ボタンを再有効化。
- スマホ版代表コースUIは変更なし。
