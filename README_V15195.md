# Traten V1.5.195

- 風速/瞬間最大風速グラフの表示上限を 10 m/s に固定。10 m/s超は上端でクリップ表示。
- Instagramリールプレビューの `response parse error` を修正。1080x1920・12秒動画の生成をHTTPリクエスト内で同期実行せず、バックグラウンド生成＋2秒間隔の状態確認へ変更。
- 管理画面APIが非JSONエラーを返した場合も `response parse error` だけでなく実際のHTTP応答内容を表示。
- リールキャッシュ版を更新し、旧生成物を誤再利用しない。
