# V1.6.90 デプロイ後チェック

1. `/api/health` で `version` が `1.6.90` であることを確認。
2. `national_cache_active_backend` が通常時 `supabase-primary` であることを確認。
3. 全国分析を開く。共有キャッシュがある日なら「全国を判定」を押さずに結果が表示されることを確認。
4. 同じステータス欄に `キャッシュ年齢 約X分 / 4時間TTL残り 約Y分` が表示されることを確認。
5. 日付を前後に切り替えても、各日の共有キャッシュ表示時にキャッシュ年齢が出ることを確認。
6. 「全国を判定」を押した後も従来どおりキャッシュ年齢が表示されることを確認。
