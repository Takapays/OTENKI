# V1.5.17 山行設定ボタン順・文言調整

## 変更
- 山行設定で「代表コースを読み込む」を先に表示。
- その下に手動設計ボタンを表示。
- 「通過ポイントを自分で設計」を「コースを自分で設計」へ変更。

## 維持
- `representativeCourseBtn` / `loadPoiBtn` のIDとイベント処理は変更なし。
- 代表コース選択、固定通過ポイント読み込み、CT計算ロジックは変更なし。
- progressive weather `analyze()` はV1.5.16とbyte-identical。
