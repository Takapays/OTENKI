# OTENKI V1.5.116

Instagramリールの全国マップに A/B/C マーカーが表示されない不具合を修正しました。

Supabaseの予報キャッシュには緯度・経度が含まれないため、リール生成前に百名山マスターの `lat` / `lon` を各結果へ補完します。あわせて生成済み動画キャッシュのバージョンを更新し、修正後のMP4を再生成します。

Renderへ差分を反映して再デプロイ後、リールURLへアクセスしてください。初回アクセスでは動画の再生成に少し時間がかかる場合があります。
