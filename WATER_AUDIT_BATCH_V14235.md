# V1.4.235 水場監査バッチ化

## 背景
V1.4.234では25座単位の途中保存を導入したが、内部では1山ごとにOverpassへ問い合わせていたため、GitHub Actionsが45分上限に到達してキャンセルされた。

## 修正
- 8座を1バッチとして1回のOverpass問い合わせに統合。
- 1問い合わせ6秒上限、最大2エンドポイント。
- GitHub Actionsは12分上限。
- バッチ失敗時は対象山を `checked:false` として保持。
- 未解決が残ってもworkflowは正常終了し、次回は未解決のみ処理。
- `OK / 0` と `ERR / 0` は引き続き区別。

## 固定ルール
- 既存固定ルート座標のみ利用。座標推測なし。
- 水場候補が取得できなかった通信エラーを「水場なし」に変換しない。
