# RELEASE AUDIT V1.5.130

## 時系列表示

- 地点別：到着前後各6時間、降水量（棒）・平均風速（線）・雷リスク（低/中/高）を同一時間軸で表示。
- 到着：到着時刻を色付き帯で強調。
- 宿泊：到着3時間前〜出発3時間後を表示し、到着から設定済み翌朝出発時刻までを色付き帯で強調。
- 複数モデルの同時刻値は平均化し、雷用CAPEは最大値を採用。

## 宿泊カード

- 夕景・天の川・朝景を比較表へ集約。
- 「朝5時の空」の独立カードを削除。
- 気温・風・雨・雲・霧・月明かりの指標カードを縮小。
- 既存の詳細分析値・快適度判定は維持。

## 回帰確認

- V1.5.129の地点分類（登山口・下山口 / 山頂 / 山小屋・峠）を維持。
- 山小屋・既存テント場のみ宿泊設定可能、峠・分岐は宿泊不可を維持。
- 残り3点代表コース：197本のまま（今回変更なし）。

## 静的監査

- `node --check app.js`: PASS
- `python3 -m py_compile server.py`: PASS
- `audit_version_consistency.py`: PASS
