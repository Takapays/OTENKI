# V1.5.9 実利用CTレビューキュー

## 目的
V1.5.8から保存している匿名の分析ルート履歴を使い、実際の山行計画で使われた区間のうち「推定CT」または「CT情報なし」を自動で抽出し、次に確定調査すべき区間を管理画面で把握する。

## 記録タイミング
個別の `weather_analysis` 成功イベント送信時に、分析した地点列の隣接区間を既存 `courseTimeInfo()` で解決する。

- 確認済み / 合成済みCT: レビュー対象にしない
- `estimated=true` または `sourceType=estimated`: `estimated` として記録
- resolverが `null`: `missing` として記録

## 保存項目
既存 `usage_events.metadata` 内の `ct_review_segments` に以下だけを保存する。

- from_name
- to_name
- status (`estimated` / `missing`)
- minutes（推定CTのみ）
- source

座標、IP、氏名、メール、User-Agent等は保存しない。

## 管理画面
「CT要確認区間」へ区間単位で集約し、以下を表示する。

- 山
- 区間
- 現在状態（推定CT / CT情報なし）
- 推定分数
- 利用回数
- ユニークセッション数
- 最終利用

利用回数が多い区間から確認することで、実利用への効果が高いCT整備を優先できる。

## 確定ルール
利用履歴は優先順位の根拠であり、CT値の根拠ではない。利用回数が多くても自動で確認済みCTへ昇格しない。公式・公的資料、YAMAP/Yamareco等の公開標準CTなど、従来の確認ルールで裏取りできた場合だけ固定CTへ昇格する。

## 過去ログ
CTレビュー情報はV1.5.9から記録するため、V1.5.8以前のイベントから「当時どの区間が推定だったか」を完全には復元しない。
