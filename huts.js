(()=>{'use strict';const db=window.TRATEN_HUT_OFFICIAL_SITES||{};const rows=Object.entries(db).map(([name,url])=>({name,url,kind:/避難小屋|石室|小屋$/.test(name)&&!/山小屋$/.test(name)?'shelter':'hut'}));const q=document.getElementById('q'),kind=document.getElementById('kind'),list=document.getElementById('list'),summary=document.getElementById('summary'),badge=document.getElementById('hutCountBadge');const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));badge.textContent=`${rows.length}件`;function render(){const term=q.value.trim().toLowerCase(),k=kind.value;const hit=rows.filter(x=>(!k||x.kind===k)&&(!term||x.name.toLowerCase().includes(term)));summary.innerHTML=`<span>登録 ${rows.length}件</span><span>表示 ${hit.length}件</span>`;list.innerHTML=hit.length?hit.map(x=>`<article class="card"><h3>${esc(x.name)}</h3><div class="meta">${x.kind==='shelter'?'避難小屋・小屋':'山小屋'}</div><a href="${esc(x.url)}" target="_blank" rel="noopener noreferrer">公式HPを開く ↗</a></article>`).join(''):`<div class="empty">該当する山小屋がありません。</div>`}q.addEventListener('input',render);kind.addEventListener('change',render);render()})();
