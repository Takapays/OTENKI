# V1.6.92 デプロイ後チェック

1. `/api/health` で `version: "1.6.92"` を確認。
2. `national_cache_engine` が `metno-gfs-jma-ridge-gust-worstof-v18-gefs-fallback` になっていることを確認。
3. `national_gefs_fallback_enabled: true` と `national_gefs_fallback_source: "NOAA GEFS 0.5 ensemble mean"` を確認。
4. 通常時は `national_cache_active_backend: "supabase-primary"` を確認。
5. エンジンID変更直後はV1.6.91以前の全国キャッシュを新判定に再利用しないため、初回は新キャッシュ生成待ちが発生し得る。
6. 今日+1〜+3日ではJMA MSM / NOAA GFSの一次稜線風が通常どおり使われ、GEFSを不要に取得していないことを確認。
7. 今日+4〜+7日では deterministic GFS上空風が正常なら、引き続きGFSが一次稜線風として表示されることを確認。
8. JMA/GFS上空風を取得できないケースでのみ、山詳細「推定稜線風」に `NOAA GEFS アンサンブル平均（補助）` が表示されることを確認。
9. GEFSだけが穏やかなケースでは、標高500m以上のAがBに抑制され、詳細に `GEFS補助のみ・A抑制` と表示されることを確認。
10. GEFSが強風を示すケースでは、安全側worst-ofとしてC/D/Eへ悪化できることを確認。
11. JMA/GFS/GEFSすべて上空風なしの場合は、10m風を稜線風へ代用せず `上空風未取得・A抑制` となることを確認。
12. 同じ欠損日を開き直しても、キャッシュが即表示され、毎回数十秒の再取得待ちにならないことを確認。
13. キャッシュ年齢 / 4時間TTL残り表示が初期表示・日付切替とも継続して表示されることを確認。
14. 9/29のようなJMA境界日のA/B/C/D/E分布を、9/28・9/30と並べて不自然な境界ジャンプがないか確認。

### 注意
全国分析のローリング自動キャッシュは1日/サイクル設定のため、新エンジンで+1〜+7日がすべて埋まるには複数サイクル必要です。確認したい日を早く作る場合は、その日を選んで「全国を判定」を一度実行してください。
